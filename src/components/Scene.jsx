import Earth from '../pages/Earth'; // Import the Earth component from pages directory

// Use default export here too
export default function Scene() {
  return (
    <div className="scene-container">
      <Earth />
    </div>
  );
}